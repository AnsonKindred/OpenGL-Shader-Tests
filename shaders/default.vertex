uniform mat3 projection;
attribute vec2 vertex;
uniform vec2 position;
attribute vec4 vertex_color;
varying vec4 color;

void main(void)  
{ 
	color = vertex_color;  
	gl_Position = vec4(projection * vec3(vertex+position, 1), 1); 
}